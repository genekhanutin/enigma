class Encrypt
	# def encrypt_message
		# Rotator.new(key value, offset, file)
		# pass in a file to rotator as the message. rotator will do the encryption, not the Encrypt class
		# Encrypt also needs to call rotator.big_ass_method
	# end
end